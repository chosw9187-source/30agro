# 프로젝트 현황 정리 (2026-08-19 기준)

다른 컴퓨터에서 이어서 작업할 때 맥락 파악용. `git clone https://github.com/chosw9187-source/30agro.git` 후 원하는 브랜치로 체크아웃하면 이 문서가 함께 따라옵니다.

## 1. hr-eval-system (한국삼공 HR 플랫폼) — `master` 브랜치 (실제 운영 사이트)

**배포**: https://30agro-production.up.railway.app, Railway Hobby 플랜(유료), `master`에 push하면 자동 배포. **주의**: `npm start`가 `prisma db push --accept-data-loss && next start`를 실행하므로, 배포되는 스키마에 없는 테이블/컬럼은 프로덕션에서 조용히 삭제됩니다. 여러 브랜치가 각자 다른 스키마를 갖고 있을 수 있으니, master에 합치기 전엔 반드시 프로덕션 실제 테이블과 대조할 것.

**레이아웃 주의**: 2026-08-20부터 데스크톱(md 이상)에서 `PlatformShell`이 화면 높이에 묶이고 `<main>`이 실제 스크롤 컨테이너가 된다(`md:h-screen md:flex-none md:overflow-hidden`). 본문 안 sticky 헤더를 쓰려면 필요한 구조다. 모바일은 기존대로 문서 스크롤. (평가2는 2026-08-24부터 고정 영역 없이 통째로 스크롤하므로 이 구조에 의존하지 않는다 — 다른 화면의 sticky가 이걸 쓴다.)

**핵심 기능**:
- **로그인/권한**: NextAuth 자격증명 로그인, 역할(ADMIN/EVALUATOR/EMPLOYEE) + 직책(사장/운영책임/책임/팀장/담당) 이중 체계. 첫 로그인 시 비밀번호 강제 변경.
- **평가 시스템**: 역량평가/성과평가 템플릿, 평가 사이클, 평가자 채점 + 본인 자기평가, 코멘트 스레드(3초 폴링), 결과 공개 토글, 레이더 차트 비교 리포트, 가중치 기반 성과 점수 계산.
- **조직도**: CEO 배너 → 사업단위/책임/팀 계층. 조직도 화면과 직원정보조회 목록에 뜨는 값(이름·직급/직책·사진·팀명·인원수·만나이·생년월일·근속연수·입사일)은 **전 직원에게 그대로 보인다** — 사용자가 명시적으로 요청한 사항이다(2026-08-20). 막히는 건 사람을 눌러 들어가는 인사카드 상세뿐이다. 한때 이 값들도 권한대로 가렸다가(912c36e) 요청에 따라 되돌렸으니, 다시 가려야 한다는 얘기가 나오면 그 커밋을 참고하면 된다. 최근에 "상세보기" 클릭 시 페이지 이동 대신 같은 화면에서 아래로 펼쳐지는 인라인 드릴다운 방식으로 개편함. 직속팀(비서실 등)은 트렁크 라인에서 옆으로 갈라지는 곁가지 형태.
- **직원정보조회 + 인사카드**: 검색/팀필터, 권한 매트릭스 기반 조회범위(전체/사업단위/부문/팀/본인/목록만/접근불가) 적용. 인사카드에 발령/학력/경력/자격/상벌사항 섹션. LIST_ONLY 직책도 본인 팀은 상세 열람 가능하도록 예외 처리됨.
- **권한 매트릭스**: 직책×모듈별 접근범위 설정 화면 + 사용자별 개별 오버라이드. **인사팀 소속 재직자 전원은 현재 role=ADMIN으로 설정되어 있음** (관리자와 동일 권한, 2026-08-19 반영). `requireRole()`이 ADMIN이면 어떤 역할 전용 화면이든 통과하도록 되어 있음.
- **인사카드(개인 상세) 열람 상한** (2026-08-20 반영): 조직도·직원정보조회에서 사람을 눌러 들어가는 개인 상세 화면은 권한 매트릭스 설정과 무관하게 직책별 상한(`POSITION_CARD_SCOPE_CEILING`)을 넘지 못한다 — 사장=전사, 운영책임=본인 사업단위, 책임=본인 부문, 팀장=본인 팀, 담당=본인만. 관리자 역할(role=ADMIN)만 전 직원 열람. 매트릭스/사용자별 설정은 이 상한을 **좁히는 방향으로만** 작동하므로, 비관리자에게 전 직원 인사카드를 열어주려면 역할을 ADMIN으로 올려야 한다. 예전에 있던 "인사팀 소속이면 직책과 무관하게 전체 열람" 예외는 이때 제거됨(인사팀 전원이 이미 ADMIN이라 실사용 영향 없음). 판정은 전부 서버 사이드라 모바일/PC가 자동으로 동일하다. 규칙의 단일 출처는 `getCardScopeFilter()`(Prisma where 조각)이고 `canViewEmployeeCard(id)`가 그 위에 얹혀 있다. 적용 지점은 인사카드 상세 화면(`/platform/employees/[userId]`) 한 곳 — 조직도·목록에서 카드를 눌러도 여기서 막힌다.
- **데이터 업로드**: ERP 일괄 임포트(변경 diff 미리보기 + 롤백 가능), 인사카드 일괄 업로드(발령/학력/경력/자격/상벌 5시트 엑셀 한 번에), 자동 업로드 API + 로컬 폴더 감시 스크립트.
- **인사카드 수동 추출 파이프라인**: ANTHROPIC_API_KEY가 없어서 서버 자동화 없이, Claude가 마스킹된 카드 이미지를 직접 읽어 수기 입력하는 방식. **161/161명 자동분류 대상 전원 완료, DB 반영 완료. 88명은 OCR 실패로 수동 크롭 목록만 전달됨 — HR팀이 직접 입력해야 함** (`수동입력_88명.xlsx`, 바탕화면). 학력사항은 이 파이프라인으로 절대 DB에 쓰지 않는 규칙(기존 데이터와 중복 발생 이력 있음).
- **관리자**: 사용자관리(엑셀 일괄가져오기/내보내기), 팀관리, 평가템플릿/사이클, 결과다운로드, 화면구성(모듈 순서/개발중 표시/홈 레이아웃), **일일 트래픽**(신규, PlatformShell에서 페이지뷰 로깅 → 일별 총조회수/순방문자 그래프+표).
- 그 외: 알림, 업무관리(Task/Subtask), SOP 문서, 근무일지, SG 인적성검사(모듈 존재, 상세 미확인).

- **평가2 (목표관리, 신규)**: 기존 "평가"는 그대로 두고 사이드바에 `평가2` 배너를 따로 추가한 새 모듈(`/platform/evaluation2`, Module=`EVALUATION_V2`). 전사목표 → 책임목표 → 팀목표 → 개인목표 4단 캐스케이드를 `Goal.parentId`로 연결하고, **달성률은 개인목표에만 사람이 입력하고, 전사·책임·팀 목표는 하위의 가중치 기준 가중평균으로만 계산된다**(하위가 없으면 0% — 아래는 비었는데 위만 100%인 표가 생기는 걸 막으려고 입력칸 자체를 없앴다). 상태를 "완료"로 두면 달성률은 100으로 본다. 담당자가 퇴사·부서이동한 목표는 `Goal.excluded`(+`excludeReason`)로 집계에서 뺄 수 있고, 화면에서 자동으로 감지해 배지와 안내를 띄운다(자동으로 빼지는 않는다 — 옮겨도 그대로 들고 가는 목표가 있어서 판단은 사람이 한다). 탭 구성은 대시보드/책임목표/팀목표/개인목표(전사목표는 탭이 아니라 어느 탭에서든 위에 고정된 표). 대시보드는 **전사목표 표(상단 고정)** + 그 아래 **층별 달성률 도넛 4장**. 상단은 두 줄이다 — **인사평가 선택 줄이 위, 층 선택 탭이 아래**(2026-08-24). 어느 해를 보는지가 먼저이고 탭은 그 해 안에서 어느 층을 볼지의 문제라서, 한 줄에 섞으면 순서가 읽히지 않는다. 인사평가 선택은 화면을 바꾸지 않고 **그 사이클의 목표로만 갈아 끼운다**. 사이클 이름·기간은 조직 목표 관리에서 그 자리에서 고친다(`renameGoalCycle`) — 지웠다 다시 만들면 그 안의 목표가 통째로 사라진다. 상태 라벨은 준비중 / 진행중 / **완료**(CLOSED). **고르기 전에는 어느 탭이든 비어 있다**(2026-08-24) — 예전에는 오늘이 속한 사이클을 알아서 잡아 줬는데, 그러면 화면의 숫자가 몇 년도 것인지 모르는 채로 읽게 된다. 2026과 2027이 나란히 열려 있는 기간에는 특히 위험하다. 사이클이 **하나도 없을 때만** 첫 실행 화면(사이클 만들기)이 뜬다 — "안 고른 것"과 "아예 없는 것"은 다르다. 층별 목록을 감싼 div에는 `key={tab}`을 준다 — 없으면 React가 같은 자리의 등록 폼을 재사용해서, 개인목표에 치던 목표명이 팀목표 탭 입력칸에 그대로 남는다. 전사목표 표는 **어느 탭에서든 펼친 채로** 열린다. **화면에 고정된 것은 없고 배너 · 전사목표 표 · 목록이 한 덩어리로 함께 스크롤된다**(2026-08-24). 고정 → sticky → 스크롤 시 접기까지 세 번 시도한 끝에 되돌린 것이라 다시 고정하려 들기 전에 이유를 읽을 것: 위를 얼려 둘수록 아래에서 볼 수 있는 자리가 줄고, "책임목표 0건 평균 달성률 0%" 같은 목록 머리글이 고정된 표 밑으로 들어가 어디로 갔는지 알 수 없게 된다. 평범한 스크롤 한 벌이면 화면 밖으로 나간 것은 위로 올리면 그대로 돌아온다. 전사목표 표는 사내 보고서 양식대로 `구분 | 목표 | 달성률` 표로 그리고(Goal.표 아래 i)/ii) 안내문 기능은 없앴다 — 아래 참조). 표는 **목표와 달성률 두 칸**이 전부다 — 구분(`Goal.category`) 칸, "완료" 배지, "하위 N건 가중평균" 문구를 차례로 뺐다. 구분은 필드째 삭제했고(2026-08-24), 순번만 목표 앞에 붙인다. 지연 배지는 남긴다, 어느 탭에 있든 화면 맨 위에 sticky로 붙는다. 전사목표 한 줄을 클릭하면 아래 3열이 그 갈래(하위 트리)만 보여준다(`?focus=<goalId>`). **조직 목표 편집은 관리자 전용 화면 `/admin/org-goals`(관리 > 조직 목표 관리)에서 한다** — 구분/목표/지표/가중치/달성률/상태/마감일을 카드 폼으로 고쳐 한 번에 저장, 목표 추가·삭제, 표 하단 안내문, 목표 사이클 관리까지 여기 모여 있다. 평가2 화면은 보기 전용에 가깝고 하단에 이 화면으로 가는 링크만 둔다. **2026-08-24부터 팀장·팀원에게도 열려 있다**(`ADMIN_ONLY_MODULES`에서 뺌). 대신 직책이 두 가지를 가른다 — (1) **탭**: 담당(팀원)에게는 책임목표 탭이 뜨지 않는다(대시보드/팀목표/개인목표만, URL로 직접 쳐도 대시보드로 되돌림). 사장·운영책임·책임·팀장·관리자는 네 탭 전부. 인사평가(사이클) 선택은 전원에게 보인다. (2) **목록 범위**: 목표 한 건씩 나오는 목록은 인사카드 열람 상한과 같은 규칙을 쓴다(`canViewGoalRow`) — 관리자·사장=전부, 운영책임=본인 본부, 책임=본인 부문, 팀장=본인 팀(이끄는 팀 포함), 담당=본인 팀의 팀목표 + 본인 개인목표. 대시보드의 층별 달성률은 조직 전체 숫자라 누구에게나 그대로 둔다. 전사목표 표 아래에는 **"지금 뭔가 어긋나 있고 이렇게 고치면 된다"는 줄만** 남긴다 — 합의 승인 대기(팀장·관리자), 담당자 퇴사·부서이동(집계에서 뺄 수 있는 사람에게만), 상위 목표 미연결. 셋 다 읽는 사람 범위의 건수로만 세고, 셀 게 없으면 회색 바 자체가 사라진다. 설명문 종류는 전부 뺐다(2026-08-24): `GoalCycle.note`(표 하단 i)/ii) 안내문) 필드와 관리 화면 폼을 삭제했고, "아직 합의되지 않은 개인목표 N건" 줄도 없앴다 — 아무도 손댈 게 없는 문장이 고정 영역에 붙어 있으면 읽히지도 않으면서 자리만 먹는다. **집계 제외는 관리자와 팀장만** 할 수 있다 — 본인이 자기 목표를 빼면 상위 달성률이 조용히 올라간다. 색은 층 식별색(globals.css의 `--color-goal-1..4` = 전사 초록 / 책임 파랑 / 팀 주황 / 개인 보라, 모든 짝 기준 CVD·대비 검증 완료)을 쓰고, 달성률 막대·도넛은 값에 따라 색을 바꾸지 않는다 — 지연/완료는 글자 배지가 맡는다. 스키마: `GoalCycle`/`Goal`/`GoalCheckIn`/`GoalCycleTarget`. **평가대상자 관리**(`/admin/eval-targets`, 관리자 전용)는 조직도와 같은 모수(재직 정규직 + 계약직 예외 팀, 명부 비공개 제외)를 본부 > 책임 > 팀으로 묶어 보여주고 사람 단위로 대상/제외를 정한다. **평가대상 여부는 저장하지 않고 조회할 때 계산한다**(`evalTargetState`) — 조직도가 바뀌거나 기준일을 고쳐도 따로 반영을 눌러줄 필요가 없다. 판정 순서는 (1) 손으로 정한 `GoalCycleTarget` 행이 이기고, (2) 없으면 `GoalCycle.hireCutoff`(입사일 기준일) — 이 날짜 **이후** 입사면 자동 제외(기준일 당일은 대상), (3) 그것도 아니면 대상. 아무것도 저장돼 있지 않은 사람이 기본으로 대상이므로 신규 입사자가 그냥 잡힌다. 계산 결과는 `GoalRow.targetExcluded`로 붙고 `countsTowardProgress`가 이를 함께 본다. 한때 사람을 빼면 `Goal.excluded`를 같이 켰는데(`TARGET_EXCLUDE_TAG`), 조직 변경마다 재반영이 필요해서 걷어냈다 — 남은 옛 표시는 이 화면에서 쓸 때 자동으로 지워진다. **사이클 간 목표 복사**는 조직 목표 관리 화면의 「이 사이클의 목표 가져오기」(`copyGoalsFromCycle`) — 층·구분·제목·담당·가중치와 상하 연결까지 복사하고 달성률·상태·합의·기한은 복사하지 않는다(새 사이클은 0%에서 시작). 이미 목표가 있는 사이클에는 안 넣는다(두 벌 생기면 가중치 합이 무너진다). **복사는 연도가 바뀔 때만 쓴다** — 같은 해 상반기/하반기는 아래 「평가 시점」으로 나눈다.

- **"기타" 묶음** (2026-08-24): 위 층 어디에도 딱 붙지 않는 일을 담는 자리. 등록 폼의 「상위 목표」에서 「기타」를 고르면 `ensureOtherGoal`이 필요한 만큼 사슬을 만든다(전사 기타 → 책임 기타 → 팀 기타). 상위를 비워 두면 아무리 달성해도 전사 달성률이 안 움직여서, 층마다 기타 한 칸을 두고 거기에 매다는 방식이다. `Goal.isOther`로 표시하고, 새로 만들 때 가중치는 형제들의 양수 가중치 평균(`defaultOtherWeight`) — 0으로 두면 가중평균이 분모에서도 빼버려 기타에 담은 일이 영영 반영되지 않는다.

- **개인목표 = 사내 「개인목표 설정」 양식(OKR)** (2026-08-24): Objective(목표)(title) · **목표 유형**(`goalType` — 업무목표/개선목표/자기계발목표) · **Key Results(핵심결과)**(`keyResults`, 줄바꿈 한 줄이 ① ② ③ 한 항목) · 가중치(비중)(weight). 유형을 안 적게 하면 개인목표가 전부 업무목표로만 차서 개선·자기계발이 목표 설정 단계에서 통째로 빠진다. Key Results를 별도 행으로 쪼개지 않은 건 양식에서도 항목별 달성률을 매기지 않기 때문 — 항목별 진척을 재게 되면 그때 행으로 쪼갠다. 목록 아래에 양식 하단의 산식(`* 점수 = 가중치(비중) × 평가자 점수`)을 띄운다. 어느 층이 이 구성을 쓰는지는 `usesKeyResults(level)` — 지금은 INDIVIDUAL만.

- **팀목표 = 사내 「팀 목표 설정」 양식** (2026-08-24): 팀목표만 보고서 양식의 칸을 그대로 갖는다 — 핵심 업무 목표(title) · 성과지표(KPI)(metric) · 목표수준 현수준/목표치(currentValue/targetValue) · 가중치(비중)(weight) · **평가척도 S(110)~D(70)**(`scaleS`~`scaleD`) · **산출식/방안**(`formula`). 평가척도는 점수가 아니라 "그 등급이 되려면 어디까지 해야 하는지"를 서술로 적는 칸이다 — 목표를 세울 때 등급 기준을 못박아 둬야 연말에 사람마다 다르게 읽지 않는다. 등급·점수는 `GOAL_SCALES` 한 곳에서 정의하고 폼·목록이 같이 쓴다. 어느 층이 이 칸을 쓰는지는 `usesScales(level)` — 지금은 TEAM만. 목록 머리글에 **가중치 소계**를 띄우고 100%가 아니면 빨갛게 표시한다(양식의 소계 줄) — `usesWeightSubtotal(level)`로 팀목표·개인목표 둘 다.

- **층별 폼 차이** (2026-08-24): **책임목표에는 가중치·측정지표·단위 칸이 없다** — 책임목표는 아래 팀목표가 굴러 올라온 값이라 지표를 따로 적을 일이 없고, 가중치를 비우면 가중평균이 동일가중으로 떨어져 부문 간 비중이 저절로 같아진다(`weightFor()`가 DIVISION을 0으로 못박는다 — 폼에 없어서 우연히 0이 되는 게 아니라 일부러 0이다). 책임목표에는 **목표수준·현재수준**도 없다(같은 이유 — 아래 팀목표가 굴러 올라온 값이라 남는 칸은 목표명·상위목표·책임·책임자·달성률·상태·마감일·설명 여덟 개뿐이다). **마감일은 세 층 모두 그 해 12월 31일이 기본값이고 고칠 수 있다** — 연중에 끝나는 목표가 있어 못박지 않는다. 책임 목록은 `DIVISION_ORDER`(제품기획마케팅 · 영업고객관리 · 기술연구 · 생산 · 재무경영관리 · 사업개발 · 기타부서)가 순서이자 기본 목록이다 — 조직도에 팀이 달려 있지 않은 책임은 팀 목록에서 유추할 수 없어 여기 없으면 고를 수가 없다. 조직도·기존 목표에만 있는 이름은 그 뒤에 가나다순으로 붙는다. 라벨은 "책임"(부문 아님).

- **필수값** (2026-08-24): 책임·팀·개인 목표는 목표명 ~ 마감일이 전부 필수(설명만 선택, 책임목표는 위에서 뺀 세 칸도 제외). 화면은 `required`로, 서버는 `requireGoalFields()`로 두 번 막는다 — 폼을 우회해 서버 액션을 부르면 브라우저 검사는 통과한다. `SearchableSelect`는 값이 hidden input에 있어 브라우저가 required를 검사하지 않으므로, 보이는 검색칸에 required + `setCustomValidity`로 "글자는 쳤는데 고르지는 않은" 상태까지 막는다. 전사목표는 관리자가 표로 채우는 방식이라 이 검사에서 제외.

- **목표 등록 폼** (2026-08-25): 층마다 칸 구성이 다르고, 순서도 사내 양식이 읽히는 차례를 따른다. **팀목표**는 「팀 목표 설정」 양식 그대로 (1) 상위 책임목표 (2) 핵심 업무 목표 · 성과지표(KPI) (3) 목표수준 현수준 · 목표치 (4) 가중치 (5) 평가척도 · 산출식/방안 (6) 상태 · 달성률 (7) 마감일 순으로 줄을 끊는다 — 상위 책임목표가 맨 위인 건 "무엇에 딸린 일인지"를 먼저 정하고 내용을 적는 순서라서다. 줄 단위로 묶으려고 각 줄을 `md:col-span-2` 안의 2열 그리드로 감싼다(`line()`). **팀·책임자(담당자)는 로그인한 사람에게서 자동으로 채운다** — 팀은 본인 팀(`viewer.teamId ?? ledTeamIds[0]`), 책임자는 **팀목표면 그 팀의 팀장**, 개인·책임목표면 본인. 이미 아는 정보를 매번 두 번씩 고르게 하면 등록이 느려지고 엉뚱한 팀을 고르는 실수도 생긴다. 남의 팀 것을 대신 등록할 일은 있으므로 칸을 없애지 않고 `<details>`로 접어 두고, 요약줄에 "기본값 재무팀 · 정팀장"을 적는다. **자동으로 못 채웠으면(팀이 없는 관리자 등) 접지 않고 펼친 채로 연다** — 접힌 `<details>` 안의 필수 칸이 비어 있으면 브라우저가 가리킬 자리를 못 찾아 저장 버튼이 먹통처럼 보인다. 이미 있는 목표를 고칠 때 배정이 기본값과 다르면 그때도 펼친다. **단위(`Goal.unit`)는 필드째 삭제**했다(2026-08-25) — 목표수준·현수준에 "2천만원"처럼 단위를 붙여 적는 게 양식이고, 칸을 따로 두면 같은 걸 두 군데 적게 된다. **팀·책임자 칸은 고를 수 있는 사람에게만 띄운다**(2026-08-25 수정) — 관리자가 아니면 서버가 어차피 로그인한 사람 기준으로 다시 정하므로(`resolveGoalScope`: 개인목표 담당자=본인, 팀목표 팀=본인이 이끄는 팀, 팀목표 책임자=그 팀장), 팀장·팀원에게는 골라도 결과가 안 바뀌는 장식이라 칸 자체를 없앴다. 예외는 팀을 둘 이상 이끄는 팀장의 「팀」 칸. 필수 검증도 폼이 아니라 **실제로 저장될 값**(`requireGoalFields(level, formData, scope)`)을 본다. **개인목표에서는 측정지표·목표수준·현재수준을 뺐다** — Key Results가 «무엇을 어디까지»를 이미 적고 있어 같은 걸 두 번 적게 된다. 이 셋은 이제 팀목표에만 있다. **평가척도 안내는 라벨 옆 빨간 물음표(`HelpMark`)에 접어 두고 마우스를 올리면 뜬다** — 괄호로 붙여 두면 칸 이름보다 안내문이 길어져 정작 무슨 칸인지가 안 읽힌다. **상위 목표 목록에는 소속(팀·부문)을 붙이지 않는다**(2026-08-26) — 고를 수 있는 상위 목표는 이미 내가 볼 수 있는 범위로 좁혀져 있어서 줄마다 «(인사팀)»이 되풀이될 뿐이다. **「기타」 묶음도 빼고**, 고르는 길은 맨 아래 「기타 (딱 맞는 상위 목표가 없을 시)」 한 줄로만 둔다 — 결과가 같은 항목이 두 줄 나란히 있으면 어느 쪽인지 헷갈린다. 단, **지금 매달려 있는 상위가 내가 볼 수 있는 범위 밖이면 그 항목은 목록에 넣어 준다**(다른 부문 책임목표에 걸린 팀목표). 없으면 select가 빈 값이 되고 필수 칸이라 저장이 아무 말 없이 막힌다 — 실제로 이것 때문에 «가중치를 고쳤는데 옛날 값이 그대로 남는» 것처럼 보였다.
- **목표설정 단계의 팀·개인 목표는 상태가 «진행중»으로 고정된다** (2026-08-26, `usesFixedActiveStatus`): 목표를 세우는 자리에서 작성중/진행중/중단을 고르게 하면 같은 시점에 세운 목표가 사람마다 다른 상태로 남아 목록이 들쭉날쭉해진다. 무엇이 끝났고 무엇이 접혔는지는 중간평가·최종평가에서 갈린다. 폼에는 고를 칸 대신 안내 줄과 숨은 값(ACTIVE)만 두고, 서버도 `statusFor`에서 한 번 더 ACTIVE로 맞춘다. 책임목표도 같이 고정한다(2026-08-26). 전사목표만 뺀다 — 관리자 화면에서 표를 채워 내려보내는 값이라 «작성중»으로 두고 다듬는 일이 있다. 단계 이름을 못 알아본 사이클은 여기서도 막지 않는다.
- **달성률은 중간평가·최종평가에서만 적는다** (2026-08-25): `allowsProgressInput(cycle)` — 단계 이름이 「목표설정」이면 달성률 입력칸도, 카드의 「진척 반영」도 뜨지 않고 서버도 받지 않는다(`createGoal`은 0으로, `updateGoal`은 지금 값을 그대로, `addGoalCheckIn`은 거절). 목표설정은 «무엇을 하겠다»를 정하는 자리라 아직 아무것도 시작하지 않았는데 숫자를 적게 하면 그 숫자가 무슨 뜻인지 아무도 모른다. 단계 이름을 못 알아본 사이클(예전 "2026년 하반기" 같은 것)은 막지 않는다. **판단 기준은 «고른 평가»이지 «목표가 저장된 사이클»이 아니다** — 목표는 대개 목표설정에 한 벌만 있고 중간·최종평가가 그걸 빌려 보므로(`sourceCycleId`), 저장 위치로 따지면 중간평가에서도 막혀 버린다. 그래서 폼이 `viewCycleId`(지금 보고 있는 평가)를 함께 보내고, 서버는 그 평가가 정말 이 목표를 다루는 평가가 맞는지 확인한 뒤 쓴다(`actingCycle`).
- **가중치·상위 연결은 관리자 전용이 아니다** (2026-08-25 버그 수정): `updateGoal`에서 `weight`와 `parentId`가 관리자에게만 저장되고 있었다. 팀장이 자기 팀 목표의 가중치를 고쳐도 조용히 옛날 값이 남았다 — 화면에는 새 숫자가 떠 있으니 «저장은 됐는데 값이 이상하다»로 보인다. 둘 다 목표를 만들 때 본인이 고르는 값이므로 수정에서도 같은 사람이 고칠 수 있어야 한다. 관리자 전용으로 남은 것은 `sortOrder`와 소속(팀·책임자)뿐이다.
- **자동 계산 층의 «완료»도 아래에서 따라온다** (2026-08-25 버그 수정, `deriveStatus`): 전사·책임·팀 목표는 달성률을 하위에서 굴려 올리는데 **상태만 사람이 고를 수 있어서** 둘이 어긋났다 — 하위가 하나도 없는 전사목표를 «완료»로 둔 한 건 때문에 달성률은 0%인데 «완료 1건»으로 세졌다. 이제 달성률이 100%가 되면 완료가 붙고, 그렇지 않은데 «완료»로 저장돼 있으면 진행중으로 되돌려 읽는다(«중단»은 사람의 판단이라 그대로 둔다). 폼과 관리자 표에서 **«완료» 선택지를 없앴고**, 서버도 자동 계산 층에는 DONE을 저장하지 않는다(`statusFor`·`companyStatus`) — 예전에 저장된 줄도 그 표를 한 번 저장하면 함께 풀린다. 개인목표는 사람이 직접 적는 층이라 그대로다.
- **개인목표 폼은 사내 「개인목표 설정」 양식 차례로** (2026-08-25): (1) 상위 팀목표 (2) 목표 구분 · 목표 유형 (3) Objective (4) Key Results (5) 가중치 · 달성률 (6) 상태 · 마감일 (7) 설명. 상위 목표가 맨 위인 건 «무엇에 딸린 일인지»를 먼저 정하고 내용을 적는 순서라서다. Key Results 라벨의 «한 줄에 하나씩 …» 안내는 뺐다 — 칸 이름보다 안내가 길면 정작 무슨 칸인지가 안 읽히고, placeholder가 이미 두 줄로 보여 준다. 팀·책임 목표와 같은 `line()` 헬퍼로 줄을 나눈다.
- **개인목표의 «1. 기본사항»** (2026-08-25): 사내 「개인목표 설정」 양식의 첫 표를 그대로 옮겼다 — **피평가자**(성명·직위·사번·소속팀·담당업무), 1차 평가자(성명·직위), 2차 평가자(성명·직위). **«본인»이 아니라 «피평가자»라고 쓴다** — 평가자와 짝이 맞는 말이라야 누가 누구를 보는지가 표 한 장에서 읽힌다. 평가2 전반에서 이 말을 쓴다. 성명·직위·사번·소속팀은 인사카드와 조직도에서, 1차 평가자는 조직도에서 한 칸 위, 2차 평가자는 그 위 한 칸(`evaluatorByPerson`을 두 번 탄다)에서 끌어온다. 종이 양식에서 매번 손으로 채우던 칸이라, 자동으로 채워 두면 목표를 세우는 사람은 목표만 적으면 된다. **담당업무만 사람이 적는다**(`GoalSheetInfo.duty`, 표 안에서 바로 저장) — 그 해 자기가 무엇을 맡았는지 본인이 적는 문장이라 인사카드에 두면 매번 인사팀을 거쳐야 한다. 평가대상 여부를 담는 `GoalCycleTarget`에 얹지 않은 이유도 분명하다: 그 표에 줄이 생기면 «손으로 정한 대상»으로 읽혀 입사일 기준(hireCutoff)을 덮어쓴다. 값은 **목표를 담은 사이클**(`sourceCycleId ?? id`)에 저장해서 목표설정·중간평가·최종평가가 같은 값을 본다. 면담일정은 넣지 않는다 — 면담 날짜는 다른 항목에서 다룬다. 표는 **로그인한 사람 기준**이고, 팀장·관리자가 남의 목표를 함께 볼 때는 카드마다 담당자 이름이 붙어 누구 것인지 갈린다.
- **개인목표에는 «팀» 칸이 없다** (2026-08-25): 사람을 고르면 그 사람의 팀이 따라온다(`teamOfOwner`). 두 칸을 다 고르게 하면 사람과 팀이 어긋난 목표가 생긴다. 관리자만 보이는 그 칸의 이름도 «담당자»가 아니라 **«피평가자»**다 — 위의 평가자와 짝이 맞는 말이라야 누가 누구를 보는지 한 번에 읽히고, «내 정보를 넣는 칸»으로 오해되지 않는다(실제로 그렇게 읽혔다). 라벨 옆에 «누구의 목표로 등록할지 고릅니다»를 붙여 둔다.
- **반기 구분** (2026-08-25, `Goal.half`): **개인목표에만 있다**(`usesHalf`, 필수) — 반기로 갈리는 건 사람이 그 기간에 무엇을 할지이고, 전사·책임·팀 목표는 한 해 동안 굴러 올라온 합이라 나눌 것이 없다(팀목표에서는 2026-08-26에 뺐다). «상반기 목표 / 하반기 목표»를 등록할 때 고르고, 목록도 그 값으로 묶여 나온다(`groupByHalf` — 상반기 → 하반기 → 미지정, 빈 묶음은 안 나온다). 한 덩어리로 늘어놓으면 지금 어느 반기 것을 보는지가 줄마다 헷갈리고 반기별 건수를 눈으로 골라내야 한다. 예전에 만든 목표는 «구분 미지정»으로 모인다 — 숨기면 어디로 갔는지 알 수 없다.
- **이어받는 평가는 앞 단계를 마감해야 열린다** (2026-08-25): 중간평가·최종평가는 앞 단계에서 **확정된** 목표를 평가하는 자리다. `sourceCycleId`가 가리키는 사이클의 `goalsLockedAt`이 비어 있으면 목록 대신 «「2026년 목표설정」이 아직 마감되지 않았습니다»와 관리자용 마감 링크를 띄운다 — 평가하는 동안 목표가 바뀌면 그 점수가 무엇을 기준으로 매겨진 것인지 남지 않는다. 그냥 비워 두면 화면이 고장 난 것처럼 보이므로 무엇을 해야 하는지까지 적는다.
- **알림은 각자 1.4초** (2026-08-25): 예전에는 맨 앞 것 하나만 시간을 재고 그게 사라져야 다음이 시작해서, 상태를 연달아 바꾸면 다섯 장이 화면 한가운데 쌓인 채 한 장씩 천천히 걷혔다. 이제 알림마다 제 시계를 갖고(성공 1.4초 / 실패 5초) 최대 3장까지만 쌓인다.
- **평가자는 조직도에서 계산한다** (2026-08-25, `src/lib/evaluator.ts`): 담당 → 본인 팀의 팀장 → 그 팀이 속한 부문의 책임 → 그 부문이 속한 본부의 운영책임 → **사장**. **회장 · 부회장은 사슬에서 뺀다**(2026-08-26) — 셋 다 조직도에서 같은 직책(`Position.CEO`)으로 들어오고 실제 자리는 직급(`jobGrade`)에 적혀서, CEO 아무나 집으면 이름순으로 먼저 오는 사람이 걸린다. 실제로 «운영책임의 평가자»가 부회장으로 나왔다. 평가는 사장이 한다. 직급이 비어 있으면 사장으로 본다 — 사슬이 통째로 끊기는 것보다 낫다. 화면 이름표도 그 자리만 직급을 쓴다(«한동우 사장»); 아래 직책은 직급이 «과장»처럼 급수라 직책을 그대로 쓴다. **사람마다 적어 두지 않고 볼 때 계산한다** — 평가대상 판정과 같은 이유로, 팀장이 바뀌거나 부서를 옮기면 평가자도 그날로 따라 바뀌어야 하는데 적어 두면 누군가 다시 눌러 주기 전까지 옛 사람이 남는다. **자기 자신이 나오면 한 칸 더 올라간다**(팀장은 본인이 그 팀의 팀장이라 그냥 두면 «내가 나를 평가»가 된다). 중간 자리가 비어 있으면(예: 그 부문에 책임이 없음) 그 칸을 건너뛰고 실제로 있는 위 사람에게 붙는다. **팀목표 카드에만** «1차 평가자: 오동률 운영책임»으로 뜬다 — 화면에 보이는 «평가자»는 언제나 **1차 평가자**를 뜻하므로 그렇게 적는다(2026-08-26). 2차 평가자는 「1. 기본사항」 표에서 본다 — 개인목표는 「1. 기본사항」이 이미 적고 있고, 책임목표는 그 목표를 누가 평가하는지가 화면에서 할 일과 이어지지 않아 뺐다(2026-08-26). 지금은 **평가자별 지정(수동 덮어쓰기)이 없다** — 필요해지면 `User`에 덮어쓰기 칸을 두고 계산값보다 우선하게 하면 된다(`evalTargetState`가 쓰는 방식과 같다).
- **사이클은 연도 단위로 한 번에 만든다** (2026-08-26, `createGoalYear`): 연도만 넣으면 «{연도}년 목표설정 · 중간평가 · 최종평가» 세 단계가 함께 생기고, 중간·최종평가는 목표설정을 `sourceCycleId`로 이어받는다. 기간은 목표설정 상반기 · 중간평가 하반기 · 최종평가 이듬해 1월로 미리 넣고 사이클 줄에서 고친다. 단계를 하나씩 손으로 만들면 «2027년은 목표설정만 있고 중간평가가 없는» 상태가 조용히 남는다. **이미 있는 단계는 건드리지 않고 빠진 것만 채우므로**, 목표설정만 있는 해에 눌러 나머지를 붙일 수 있다(그 해 목표는 그대로). 만든 뒤 그 해 단계의 순번을 목표설정 → 중간평가 → 최종평가로 다시 세운다 — 빠진 것만 채우다 보면 예전 순번과 뒤섞여 «중간평가 · 최종평가 · 목표설정»으로 읽히는 해가 생긴다. 이름·기간을 직접 넣던 `createGoalCycle`은 부르는 화면이 없어져 지웠다.
- **중단 처리** (2026-08-26, `setGoalDropped`): 목표 카드에서 **관리자만**, **중간평가·최종평가에서만** 누른다. 연중에 접은 목표를 남겨 두면 «달성 못 한 목표»로 계속 세어져 팀 달성률을 끌어내리는데, 중단이면 집계에서 통째로 빠진다(`countsTowardProgress`). 목표를 확정(마감)한 뒤에도 눌러야 하므로 내용 잠금(`canEditGoals`)이 아니라 진척 잠금(`canEditProgress`)을 따른다 — 접히는 건 대개 마감 이후에 벌어지는 일이다. 중단을 풀면 «진행중»으로 되돌린다(접기 전 상태를 되살리지 않는다 — 다시 굴러가는 시점에는 진행중이 맞고, 완료 여부는 달성률에서 따라온다). 카드의 누르는 버튼들은 오른쪽 묶음 하나에 모아서, 어떤 버튼이 보이느냐에 따라 줄이 갈라지지 않게 한다.
- **평가자 확인 표** (2026-08-26, 평가대상자 관리): 전 직원의 1차·2차 평가자를 한 표에서 훑고, **조직도가 비어 사슬이 사장까지 올라간 사람**을 빨갛게 짚어 준다(«조직도에 부문 「경영지원책임」의 책임, 본부 「경영본부」의 운영책임이 없어 사장으로 올라갔습니다»). 목표 화면에서는 «평가자: 사장»만 보여서 무엇이 비었는지 알 수 없었다 — 실제로 «팀장 평가자가 왜 사장이냐»는 물음이 여기서 나왔고, 답은 그 팀의 부문·본부를 맡은 사람이 조직도에 없다는 것이었다. 두 가지를 함께 고쳤다: (1) **사슬은 조직 전체로 만든다** — 평가대상자 명단(계약직·명부 비공개를 거른 것)으로 만들면 걸러진 사람이 맡은 자리가 통째로 비어 보인다(사장이 명단에서 빠져 운영책임들의 평가자가 «찾지 못함»으로 나왔다). (2) **자기 층 이하의 빈 자리는 세지 않는다** — 운영책임에게 «부문 책임이 없다»고 알리는 건 틀린 말이다. 운영책임의 다음 칸은 원래 사장이다. 책임·운영책임이 **맡은** 조직은 본인 인사카드의 부문·본부를 먼저 본다(어쩌다 팀에 소속돼 있다고 그 팀의 부문으로 바뀌면 엉뚱한 사람이 평가자가 된다). 반대로 **속한** 조직은 팀 쪽이 먼저다(팀 이동 뒤 개인 값이 남아 있을 수 있다).
- **단계는 앞 단계에서 이어받는다** (2026-08-25, `previousPhaseSource`): 새 사이클을 만들 때 「목표 공유」를 고르지 않으면 **같은 해의 바로 앞 단계**가 자동으로 잡힌다(중간평가·최종평가 → 그 해 목표설정). 고르는 걸 잊으면 빈 화면이 열리는데 왜 비었는지는 화면에 안 적혀 있었다. 사슬은 한 단계만 허용하므로 앞 단계가 이미 남의 목표를 빌려 쓰면 그 원본을 가리킨다 — 중간평가도 최종평가도 결국 목표설정 한 곳을 본다. 상단 배지도 «「2026년 목표설정」의 목표를 이어받습니다»로 바꾸고, **앞 단계가 아직 마감 전이면 그 말을 함께 적는다** — «이어받았다»만 있으면 지금 보는 숫자가 확정된 것인지 아직 고치는 중인지 알 수 없다.
- **목표 카드에 남기는 것** (2026-08-25): 제목 줄에는 이름 · 상태 배지 · (개인목표만) 소속 · 오른쪽 끝에 달성률. **«하위 목표가 없어 0%입니다»는 그 달성률 바로 앞**에 붙고 0%일 때만 뜬다 — 아래 버튼 줄에 두면 숫자와 설명이 멀어 0%만 보고 «고장인가»가 된다. 그 아래 줄에는 **상위 목표와 가중치 둘만** 둔다. 지표·목표수준·현수준·산출식·마감일·하위 건수까지 늘어놓으면 한 줄이 화면을 가로질러서 정작 무엇에 딸려 있고 얼마나 무거운지가 안 읽힌다(자세한 값은 «수정»을 눌러 폼에서 본다). **고치고 지우는 버튼은 오른쪽 끝**에 모은다 — 왼쪽은 상태를 읽는 자리, 오른쪽은 누르는 자리다. 소속은 개인목표에만 붙인다: 책임목표·팀목표 탭은 그 자체가 부문별·팀별 목록이라 줄마다 같은 이름이 되풀이될 뿐이다.
- **전사목표 표는 누르는 자리가 아니다** (2026-08-25): 한때 한 줄을 눌러 «이 갈래만 보기»(`?focus=`)로 걸렀는데, 한 번 누르면 아직 아무것도 안 달린 책임목표가 나와 비어 보이고 다시 눌러야 원래대로 돌아와서 화면이 왜 바뀌었는지 알기 어려웠다. 링크와 `focus` 파라미터를 통째로 걷어냈다 — 아래 층과의 연결(달성률이 굴러 올라오는 것)은 그대로고, 전사목표를 고치는 일은 관리자 화면(`/admin/org-goals`)에서 한다.
- **머리글 건수는 전사목표만 센다** (2026-08-25 버그 수정): «2026년 전사 목표» 머리글의 목표·완료·지연·제외가 네 층을 전부 세고 있어서, 전사목표 6건 중 완료가 하나도 없는데 «완료 1»이 떴다(아래층 개인목표 한 건이었다). 옆의 «전사 종합 %»가 전사목표 기준이므로 한 줄이 같은 것을 말하도록 맞췄다.
- **사람 고르는 칸은 «이름 직책(소속)»** (2026-08-25): «오동률 책임(재무경영관리)», «조성완 팀장(인사팀)». 이름만으로는 동명이인을 가릴 수 없고, 누구를 골라야 하는지가 직책에서 읽힌다. 검색이 라벨을 훑으므로 «책임»·«팀장»으로도 찾힌다.
- **팀목표 가중치는 개인목표에서 굴려 올린다** (2026-08-25, `usesDerivedWeight`): 팀 목표의 비중은 결국 딸린 개인목표들이 짊어진 몫의 합이라, 손으로 따로 적게 하면 개인목표를 더하고 뺄 때마다 팀목표 가중치를 같이 고쳐야 하는데 아무도 그러지 않아 둘이 어긋난 채로 남는다. 폼에서 가중치 칸을 빼고(«딸린 개인목표의 가중치 합으로 자동 계산됩니다»), `buildGoalTree`가 `GoalNode.rollupWeight`을 함께 계산한다 — **집계에서 빠진 개인목표(중단·집계 제외·평가대상 아님)는 그 몫도 같이 빠진다**. 달성률에서 빼놓고 비중만 남기면 그 팀 목표가 실제보다 무겁게 잡힌다. `weightedProgress`도 `weight`가 아니라 `rollupWeight`을 본다(다른 층은 둘이 같은 값). 저장된 `Goal.weight`은 팀목표에서 0으로 두고 아무도 안 본다 — 달성률과 평가대상처럼 **계산해서 읽는 값**이다. 팀목표의 «가중치 소계 100%» 줄은 없앴다(`usesWeightSubtotal`은 이제 개인목표만) — 사람이 맞출 수 없는 숫자에 100%로 맞추라고 하면 고칠 수 없는 걸 붙들고 있으라는 뜻이 된다. **사람마다 100씩 똑같이 가져간다**(2026-08-25, `PERSON_SHARE`·`personShares`) — 개인목표 가중치를 그대로 쓰면 목표를 적게 잡은 사람이 팀 달성률에 덜 반영되는데(합이 50인 사람은 100을 채운 사람의 반), 그 50이 «일이 적어서»인지 «칸을 덜 채워서»인지는 화면 어디에도 없다. 알 수 없는 것을 근거로 사람마다 다른 무게를 주느니 다섯 명이면 다섯 몫으로 나눈다. 그래서 **적어 넣은 가중치는 그 사람 안에서 나누는 비율로만** 쓴다 — 30·20을 적었으면 60·40으로 펴져 합이 100이 되고, 이미 100을 맞춘 사람에게는 아무 일도 일어나지 않는다. 몫은 **사람 단위로 전체에 걸쳐** 나눈다: 한 사람의 개인목표가 여러 팀목표에 걸쳐 있으면 그 사람의 100이 그만큼 쪼개진다(팀목표를 더 만든다고 그 사람 영향력이 커지지 않는다). 집계에서 빠진 목표는 나눗셈에도 안 들어가서, 두 건 중 하나가 빠지면 남은 하나가 그 사람 몫 100을 통째로 진다. **화면 표시는 다르다** — 팀목표 카드는 굴려 올린 몫(사람 수 × 100), 개인목표 카드는 **사람이 적어 넣은 값 그대로**. 펴 놓은 몫을 개인목표에 띄우면 30을 적었는데 60으로 보인다. «가중치 소계»도 적어 넣은 값으로 세고 **한 사람 것만 볼 때만** 띄운다 — 팀장이 팀원 다섯 명 것을 한 화면에서 보면 500%가 되는데 «100%로 맞춰 주세요»를 붙이면 맞출 수 없는 걸 맞추라는 말이 된다. 여러 사람이 섞이면 «소계가 100%가 아닌 사람 N명»만 띄운다.
- **집계 제외는 관리자만** (2026-08-25 변경): 한때 팀장에게도 열려 있었으나 닫았다 — 목표를 세우는 사람 손에 «내 숫자를 좋아 보이게 하는 버튼»을 쥐여 주는 꼴이다. 팀장·팀원 카드에는 **수정과 삭제**만 남는다. 대신 **삭제는 그 목표를 맡은 사람이면 할 수 있다**(관리자 전용에서 `canManageGoal`로) — 자기가 잘못 만든 목표를 지우려고 관리자를 찾아가야 하면 제목만 «(취소)»로 바꾼 껍데기가 쌓인다. 합의 완료된 목표는 수정과 마찬가지로 담당자 혼자 못 지운다.
- **카드에서 뺀 것들** (2026-08-25): 마감일과 하위 건수. 목표마다 거의 다 «마감 12. 31.»이라 줄만 길어지고, 하위 건수는 바로 아래 달성률 안내가 이미 말해 준다. 늦은 목표는 제목 옆 «지연» 배지가 알려 준다. 자동으로 만들어지는 「기타」 자리는 이름을 **「기타 목표」**로 바꾸고 설명문(«위 층 목표에 직접 붙지 않는 일을 모아 두는 자리입니다»)을 없앴다 — 이름이 이미 그 뜻이다. 예전에 «기타»로 저장된 줄은 화면에서 읽을 때 새 이름으로 맞춘다(`goalTitle`).
- **숫자 칸은 방향키·휠에 값이 바뀌지 않는다** (2026-08-25, `NumberStepGuard`): `<input type="number">`의 브라우저 기본 동작이 조용히 값을 갉아먹는다 — 가중치에 30을 적고 아래로 내려가려고 ↓를 한 번 누르면 **페이지는 그대로 있고 값만 29**가 된다. 두 번이면 28. «저장되었습니다»는 정상으로 뜨니까 적은 값과 다른 숫자가 남고, 나중에 «분명 30을 적었는데»가 된다. 실제로 30→29, 15→13/14로 저장되는 일이 있었고 그대로 재현했다. 방향키(↑↓·PageUp/Down)가 오면 값을 바꾸지 않고 그 칸에서 빠져나오고, 휠은 포커스를 푼다(크롬은 이제 휠로 안 바뀌지만 파이어폭스는 바뀐다). **플랫폼 껍데기(`PlatformShell`)에 한 번 붙여 앱 전체의 숫자 칸을 한꺼번에 지킨다** — 칸마다 챙기게 두면 새로 만드는 폼에서 또 빠진다. 화살표로 숫자를 한 칸씩 올리는 기능은 없어지지만, 퍼센트는 눌러서 맞추는 값이 아니라 적어 넣는 값이다.
- **목표 카드에 부문 이름은 안 붙인다** (2026-08-25): 책임목표 탭은 그 자체가 부문별 목록이라 «재무경영관리»가 줄마다 되풀이될 뿐이고 목표 이름 옆자리를 먹는다. 팀목표의 팀 이름과 개인목표의 담당자 이름은 남긴다 — 여러 팀·여러 사람 것이 한 목록에 섞여 나오므로 누구 목표인지 가려 주는 유일한 표시다.
- **알림은 화면 한가운데** (2026-08-25): `ToastHost`가 `fixed inset-0 … justify-center`. 맨 위에 붙여 두면 아래쪽에서 저장 버튼을 누른 사람 눈에 안 들어온다 — 방금 누른 자리에서 멀수록 못 본다.
- **인사평가 순서** (2026-08-24): `GoalCycle.sortOrder`. 조직 목표 관리 > 목표 사이클에서 ▲▼로 옮기며, 옮길 때마다 전체 순번을 1부터 다시 매긴다(두 줄만 맞바꾸면 아직 순서를 정한 적 없는 0끼리 순서가 안 정해진다). 정렬 규칙은 `GOAL_CYCLE_ORDER` 한 군데 — 평가2 선택 배너, 조직 목표 관리, 평가대상자 관리가 같이 쓴다. 화면마다 순서가 다르면 "위에서 몇 번째"라는 말이 통하지 않는다. 연도·기간순만으로는 "목표설정 → 상반기 → 최종평가"처럼 일이 진행되는 순서로 못 늘어놓아서(상반기가 날짜로는 앞선다) 손으로 정하는 순번이 필요했다. **연도 > 단계 2단 구조**(2026-08-25): 사이클 목록은 어디서나 연도로 묶여 나오고, 그 안에 단계만 남는다 — 조직 목표 관리는 `2026년` 제목 아래 세 줄, 평가2 첫화면의 [인사평가 - 선택]은 `<optgroup>` 안에 `목표설정 / 중간평가 / 최종평가`로 뜬다(`groupCyclesByYear`·`cyclePhaseLabel`). "2026년 목표설정 / 2026년 중간평가 / 2026년 최종평가"가 평평하게 늘어서면 몇 해치가 섞인 순간 읽기 어려워지는데, 연도 아래 단계가 들어가면 "2026년에는 이 셋이 있다"가 한눈에 읽힌다. **연도는 이름 앞머리를 먼저 본다**(`cycleYear` → `parseNameYear`) — 기간에 실수로 다른 해를 넣어도(실제로 중간평가에 2028, 최종평가에 2029이 들어가 있었다) 화면에서는 이름대로 묶이고, 그 사이클을 다시 저장할 때 `GoalCycle.year`도 이름 기준으로 맞춰진다(`createGoalCycle`·`renameGoalCycle`). 같은 해 안 순서는 `sortOrder`가 먼저이고, **서로 같아 순서를 가릴 수 없을 때만 단계 순위**(목표설정 → 중간/상반기 → 최종/하반기, `cyclePhaseRank`)가 걸린다 — 새로 만든 사이클은 전부 같은 값(9999)으로 들어와서, 이게 없으면 최종평가가 중간평가 위에 붙는다. ▲▼는 **같은 해 안에서만** 옮기고(해를 넘기면 묶음이 통째로 뒤바뀐다) 그 해가 쓰던 순번 자리만 다시 채운다. `moveGoalCycle`도 `groupCyclesByYear`를 거친 **화면과 같은 차례**로 계산한다 — DB에서 읽은 차례를 그대로 쓰면 화살표가 화면에 보이는 것과 다른 줄을 집는다.

- **사이클 간 목표 공유** (2026-08-24): `GoalCycle.sourceCycleId`. 같은 해 목표 한 벌을 여러 평가가 나눠 볼 때 쓴다 — "2026년 상반기"·"2026년 최종평가"가 "2026년 목표설정"의 목표를 **그대로 참조**한다(복사 아님). 복사하면 어느 쪽이 진짜인지가 생기고 한쪽에서 고치면 어긋나는데, 참조는 화면만 여러 개고 목표는 한 벌이라 한쪽에서 진척을 올리면 다른 쪽도 같이 움직인다. **연도가 바뀔 때는 참조가 아니라 복사가 맞다**(`copyGoalsFromCycle`). 사슬은 한 단계만 허용(`resolveShareSource`). 목표를 읽고 쓰는 모든 곳이 `sourceCycleId ?? id`를 쓴다 — 평가2 화면, 조직 목표 관리, 평가대상자 관리, 평가 시점 스냅샷. 잠금도 목표를 담은 쪽을 따른다(서버가 그렇게 판단하므로, 다르게 하면 눌리는데 저장은 안 되는 버튼이 생긴다). 설정은 조직 목표 관리 > 목표 사이클 줄의 「목표 공유」와 새 사이클 만들기 폼.

- **마감(잠금) 2단계** (2026-08-24): `GoalCycle.goalsLockedAt`을 찍으면 **목표 확정** — 제목·지표·가중치·담당·구조를 아무도 못 고치고 진척(달성률)만 올린다. 목표를 다 세운 뒤 목표가 슬그머니 바뀌면 평가의 기준 자체가 흔들리기 때문이다. 사이클 `status=CLOSED`면 **완전 읽기 전용**(진척·합의까지 잠김). **관리자도 예외가 아니다** — 마감을 풀어야 고칠 수 있다. 규칙의 단일 출처는 `cycleLock()`이고, 서버 액션은 `requireCycleEditable(cycleId, "goal" | "progress")`로, 화면은 `lock.canEditGoals` / `lock.canEditProgress`로 같이 막는다. 마감 뒤에도 「진척 반영」은 살아 있어 100% 찍으면 완료 처리까지 된다(`addGoalCheckIn`). 마감/해제는 조직 목표 관리 > 목표 사이클 줄의 버튼.

- **평가 시점(스냅샷)** (2026-08-24): `GoalCheckpoint` + `GoalCheckpointEntry`. "2026년 상반기 평가"처럼 한 사이클 안에서 성적을 끊어 읽을 때, 그 순간의 **굴려 올린** 달성률을 목표별로 얼려 저장한다(저장된 progress를 그대로 넣으면 전사·책임·팀이 전부 0으로 찍힌다). 하반기에 숫자가 더 올라가도 상반기 성적은 그대로다. 상반기 평가를 하겠다고 목표를 통째로 복사해 새 사이클을 만들지 **않는** 이유가 이것 — 복사하면 같은 목표가 두 벌이 되어 어느 쪽이 진짜인지가 생기고, 목표 하나의 1년치 진척 이력도 끊긴다. 편집 권한은 관리자 전권, 팀장은 자기 팀 팀목표, 그 외에는 본인 개인목표와 담당 목표의 진척 반영만.

**한 번 참고할 것**: production DB에 스키마엔 없는데 실제로 존재하는 테이블이 종종 생김(다른 브랜치가 먼저 db push 해둔 경우) — master에 배포하기 전엔 `information_schema.tables`로 대조 확인 권장.

## 2. 직무분석 프로그램 — `wip/job-analysis-onboarding` 브랜치

팀별 "단위업무" 등록 화면. 팀마다 업무 항목(구분/업무명/상세/필요등급/산출물/관련부서/법적근거/빈도/연간횟수/1회소요시간/수행인원)을 등록·수정. 엑셀 업로드 시 열 매핑을 사람이 확인 후 저장(자동저장 아님), 기존 직무기술서 텍스트에서 초안 자동 생성 기능도 있음. 팀 간 중복 업무명 감지 기능 포함.
**미완성 부분**: 빈도/연간횟수/소요시간/인원 데이터로 FTE(적정인원) 산출하는 로직은 스키마 주석에 "다음 단계에서 붙인다"고 명시 — 아직 없음.

## 3. SG 온보딩 프로그램 스케줄 예약 — `deploy/onboarding-schedule` 브랜치

신입사원 온보딩 교육 예약 시스템. 관리자가 기수(OnboardingProgram, 기간 지정) 생성 → 기존 직원 중 강사(OnboardingInstructor) 지정 → 관리자/강사가 교육 세션(OnboardingSession: 제목/장소/시작·종료시간/필요강사수) 등록 → 강사가 스스로 예약 신청(OnboardingBooking, REQUESTED 상태) → 관리자가 승인/거절. 탭 4개: 일정관리/강사지정(관리자전용)/온보딩일정(전체)/내 강의일정(강사).
**상태**: 조사한 세 브랜치 중 가장 완성도 높음 — TODO/미완성 표시 없음, 권한체크 전 구간 적용됨.

## 4. 인사 규정 챗봇 — `deploy/regulation` 브랜치

이름은 "챗봇"이지만 **실제로는 챗봇이 아직 아님** — 키워드 검색 도구. 관리자가 PDF/DOCX/TXT 규정 파일 업로드 → 텍스트 추출 → "제N조" 패턴으로 조문 단위 파싱해서 저장 → 사용자는 제목/본문/장(chapter)/라벨에 대해 단순 부분일치 검색만 가능. LLM 호출이나 벡터 검색은 전혀 없음 — 화면에도 "자연어로 묻고 답하는 기능은 준비 중입니다"라고 명시되어 있음. 코드 주석에 2단계 LLM 설계(목차→관련조문 선택→답변) 구상은 있으나 미구현.

## 5. 병해충진단앱 (SG_영업사원 작물보호제 학습 도우미) — 별도 저장소

경로: `C:\Users\sg\앱들기\병해충진단앱`, GitHub: `sg-crop-protection-app` (main 브랜치, 이미 전부 push됨, 이 저장소는 위 hr-eval-system과 무관한 별개 프로젝트).

신입/저연차 영업사원용 작물보호제 학습 도구. 단일 HTML 파일로 동작(서버 불필요, 오프라인 가능). 메뉴: 검색진단(병해충/잡초/작물 검색), "Hi, SG" 음성 질의응답(로컬 제품 데이터 키워드 매칭 — 실제 LLM API 호출 아님), 업무일지(Hi,SG 질의응답 자동기록), 역량퀴즈(제품 데이터 기반 자동생성), 제품정보+혼용/희석 계산기, 제형 설명. 접속코드(`SG2026`, app_config.json)로 간단히 잠겨있음(진짜 보안 아니라고 README에 명시).
**미완성 부분**: 사진 촬영으로 병해충 AI 진단하는 기능은 "준비 중" 배너만 있고 미구현.

---
*이 문서는 Claude가 세션 간 인수인계를 위해 작성. 부정확한 부분 발견하면 직접 고치거나 다음 세션에 알려주세요.*
